export interface TeamComp {
  region: string;
  gameType: string;
  teamCompName: string;
  gameTier: string;
  result: boolean;
}

export interface CompCards {
  selectedRegion: string;
  selectedMode: string;
  selectedOption: string;
  selectedRank: number;
}
